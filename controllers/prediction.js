var express = require('express');

/**********************************
 *
 * @param arr //an array containing an object with X and Y values
 * @return newArr //a new array that contains the predicted values based on the average gradient
 */
var predictor = function(arr){

   // arr[0].x;
   // arr[0].y;
    //arr.length;
};

module.exports = {
    predictor: predictor

};